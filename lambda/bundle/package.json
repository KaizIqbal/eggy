{
  "name": "bundle",
  "version": "1.0.0",
  "author": "KaizKhatri",
  "license": "MIT",
  "private": true,
  "scripts": {
    "bundle:local": "npx sls invoke local -f bundle ",
    "bundle:dev": "npx sls invoke -f bundle -l ",
    "bundle": "npx sls invoke -f bundle ",
    "deploy:dev": "npx sls deploy -v",
    "deploy": "npx sls deploy -s",
    "clean": "npx sls remove"
  },
  "dependencies": {
    "serverless": "^1.70.0",
    "serverless-python-requirements": "^5.1.0"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^2.4.2"
  }
}
